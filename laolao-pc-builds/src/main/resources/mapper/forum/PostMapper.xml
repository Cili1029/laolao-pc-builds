<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laolao.mapper.forum.PostMapper">
    <update id="updateCommentCount">
        UPDATE forum_post
        <set>
            <if test="delta != null">
                comment_count = comment_count + #{delta},
            </if>
            <if test="now != null">
                updated_at = #{now}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <select id="getPostBatch" resultType="com.laolao.pojo.forum.entity.Post" parameterType="com.laolao.pojo.forum.entity.Post">
        select *
        from forum_post
        where status = 1
        and id in
        <foreach collection="postIdList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
